apiVersion: v1
kind: ConfigMap
metadata:
  name: example-app-config
  namespace: example-app
  labels:
    app: example-app
    managed-by: argocd
data:
  app.properties: |
    environment=production
    log.level=info
    feature.enabled=true

